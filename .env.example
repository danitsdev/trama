# Copie para `.env.local` no desenvolvimento, sem versionar credenciais.
# No Vercel, conecte o Neon para que DATABASE_URL seja injetada automaticamente.
DATABASE_URL=postgresql://USER:PASSWORD@HOST/DATABASE?sslmode=require
